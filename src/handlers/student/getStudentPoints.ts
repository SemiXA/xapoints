import express from 'express';
import { Student } from '../../model/types/student';
import {findOneStudent} from '../../model/services/studentServices';


async function getStudentPoints(req: express.Request, res: express.Response){
    
 try {
    const studentId = "1";
    
 } catch (error) {
    
 }
}

export {getStudentPoints};